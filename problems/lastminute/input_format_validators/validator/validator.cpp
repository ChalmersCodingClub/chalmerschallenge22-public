#include "validator.h"
#include <cmath>

#define sz(v) int(v.size())

void run() {
	int a = Int(0, 1e9);
	Space();
	int b = Int(0, 1e9);
	Space();
	int c = Int(0, 1e9);
	Space();
	int d = Int(0, 1e9);
	Endl();
	Eof();
}
