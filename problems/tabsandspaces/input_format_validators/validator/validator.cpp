#include "validator.h"
#include <cmath>

#define sz(v) int(v.size())

void run() {
	int f = Int(1, 10); Endl();
	for(int i = 0; i < f; i++){
		int l = Int(1, 20); Endl();
		for(int j = 0; j < l; j++){
			Int(0, 79); Endl();
		}
	}
	Eof();
}
